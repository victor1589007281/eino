apiVersion: v1
kind: Service
metadata:
  name: {{ include "image-gen-agent.fullname" . }}
  labels:
    {{- include "image-gen-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "image-gen-agent.selectorLabels" . | nindent 4 }}
