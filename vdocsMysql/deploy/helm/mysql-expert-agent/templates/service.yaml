apiVersion: v1
kind: Service
metadata:
  name: {{ include "mysql-expert-agent.fullname" . }}
  labels:
    {{- include "mysql-expert-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.service.metricsPort }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "mysql-expert-agent.selectorLabels" . | nindent 4 }}
